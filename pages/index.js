import { Button } from '../components/Button'

const index = () => {
  return (
    <div>
      <Button
        label="Button"
        onClick={() => {}}
        size="large"
      />
    </div>
  );
}

export default index;